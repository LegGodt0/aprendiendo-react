import React from 'react' // Importar react puesto que está instalado
import ReactDOM from 'react-dom/client' // Importar el DOM
import {App} from './App.jsx'
import './index.css'

const root = ReactDOM.createRoot(document.getElementById('root'))

root.render(
  <App />
)
